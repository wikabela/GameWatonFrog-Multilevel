using UnityEngine;
using UnityEngine.SceneManagement;

public class MainLagi : MonoBehaviour
{
    // Start is called before the first frame update
public void GoToGo(){
    SceneManager.LoadScene(0);
}
}
